import 'package:flutter/material.dart';

// Basket Page CLASS
class BasketPage extends StatelessWidget {
  const BasketPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Basket Page'));
  }
}